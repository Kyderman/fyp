<div class="nested-fields">
  <%= f.association :user, collection: User.where(role: 0) %>
  <%= f.input :is_owner %>
  <%= link_to_remove_association "Remove", f, :class => 'btn btn-danger btn-block' %>
  <br>
</div>